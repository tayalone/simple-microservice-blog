apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: ingess-srv
  annotations:
    kubernates.io/ingess.class: nginx
    nginx.ingress.kubernetes.io/use-regex: "true"
spec:
  rule:
    - host: posts.com
      http:
        paths:
          - path: /posts/create
            backend:
              serviceName: posts-clusterip-srv
              servicePort: 4000
          - path: /posts/create
            backend:
              serviceName: query-srv
              servicePort: 4002
          - path: /posts/?(.*)/comments
            backend:
              serviceName: comments-srv
              servicePort: 4001
          - path: /?(.*)
            backend:
              serviceName: client-srv
              servicePort: 3000
